apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: celery-worker
  labels:
    app: celery-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-worker-container
  template:
    metadata:
      labels:
        app: celery-worker-container
    spec:
      containers:
        - name: celery-worker
          image: gitumarkk/k8_django_minikube:part_4
          command: ['celery', '-A', 'kubernetes_django', 'worker', '-l', 'info']
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: user

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password

            - name: POSTGRES_HOST
              value: postgres-service

            - name: REDIS_HOST
              value: redis-master
